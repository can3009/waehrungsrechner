import 'package:flutter/material.dart';

class DrawerAboutApp extends StatelessWidget {
  const DrawerAboutApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('About App'),
      ),
    );
  }
}
